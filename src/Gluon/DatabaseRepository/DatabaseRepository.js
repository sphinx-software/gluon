/**
 * @implements RepositoryInterface
 */
export default class DatabaseRepository {

    remove(identifier) {
    }

    get(identifier) {
    }

    create(modelProperties) {
    }

    all() {
    }

    save(model) {
    }

    find(condition) {
    }

    first() {

    }
}
